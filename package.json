{
  "scripts": {
    "start": "vite",
    "host": "vite --host",
    "build": "vite build",
    "preview": "vite build && vite preview",
    "deploy": "vite build --base=/block-buster-prototype/ && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "vite": "^4.1.4"
  },
  "dependencies": {
    "firebase": "^9.17.2",
    "lit-html": "^2.6.1"
  }
}
